# Server configuration
PORT=3000

# Directory where repositories are stored (default: ./repos)
REPOS_DIR=/path/to/repos

# GitHub webhook secret (optional - will be auto-generated if not provided)
# This must match the secret configured in your GitHub webhook settings
GITHUB_WEBHOOK_SECRET=your-github-webhook-secret-here

# Default branch to checkout when cloning repositories (default: main)
# This is used as fallback if GitHub doesn't provide the default branch
DEFAULT_BRANCH=main

# Auto-clone repositories when webhook is received (default: true)
# Set to false to require manual cloning
AUTO_CLONE=true
